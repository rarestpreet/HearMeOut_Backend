spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/hearmeout
    username: root
    password: mysecretpassword
    hikari:
      initialization-fail-timeout: 6000


  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: redis
      port: 6379

  rabbitmq:
    host: rabbitmq